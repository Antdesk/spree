<% content_for :page_title do %>
  <%= Spree.t(:listing_reports) %>
<% end %>

<table class="index">
  <thead>
    <tr data-hook="reports_header">
      <th><%= Spree.t(:name) %></th>
      <th><%= Spree.t(:description) %></th>
    </tr>
  </thead>
  <tbody>
    <% @reports.each do |key, value| %>
    <tr data-hook="reports_row">
      <td class="align-center"><%= link_to Spree.t("sales_totals"), send("#{key}_admin_reports_url".to_sym) %></td>
      <td style="padding-left:1em"><%= Spree.t("sales_total_description") %></td>
    </tr>
    <% end %>
  </tbody>
</table>
